## How to Run Locally
Clone the repository or download the ZIP file.

Open index.html in any modern web browser (Chrome, Edge, Firefox).

No local server or npm install is required—it runs directly in the browser.

## Project Structure
index.html: The game container and canvas element.

style.css: Simple layout to keep the game responsive.

game.js: The main engine containing the update() and draw() loops.
